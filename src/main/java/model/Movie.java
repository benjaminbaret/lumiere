package model;

public class Movie {
    private int m_id, m_status;
    private String m_title, m_synopsis, m_director, m_genre;
    //SimpleDateFormat m_dateFormat = new SimpleDateFormat("HH:mm:ss");
    //String m_timeLast = m_dateFormat.format(new Date());

}
